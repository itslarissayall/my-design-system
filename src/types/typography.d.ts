/**
 * Font size tokens in pixels.
 *
 * @type {FontSizeTokens}
 */
export type FontSizeTokens = {
  /**
   * 2xs - Extra, extra small font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 8px;
   * ```
   */
  '2xs': { value: string }
  /**
   * xs - Extra small font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 12px;
   * ```
   */
  xs: { value: string }
  /**
   * sm - Small font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 14px;
   * ```
   */
  sm: { value: string }
  /**
   * md - Medium font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 16px;
   * ```
   */
  md: { value: string }
  /**
   * lg - Large font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 18px;
   * ```
   */
  lg: { value: string }
  /**
   * xl - Extra large font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 20px;
   * ```
   */
  xl: { value: string }
  /**
   * xl - Extra large font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 24px;
   * ```
   */
  '2xl': { value: string }
  /**
   * xl - Extra large font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 30px;
   * ```
   */
  '3xl': { value: string }
  /**
   * 4xl - 4 Extra large font size.
   * @type {string}
   * @example
   * ```css
   * font-size: 36px;
   * ```
   */
  '4xl': { value: string }
  /**
   * 5xl - 5 Extra large font size.
   * @type {string}
   * @example
  * ```css
   * font-size: 48px;
   * ```
   */
}

export type FontWeightsTokens = {
  /**
   * Thin font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 100;
   * ```
   */
  thin: { value: string }
  /**
   * Thin font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 100;
   * ```
   */
  extralight: { value: string }
  /**
   * Extralight font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 200;
   * ```
   */
  light: { value: string }
  /**
   * Light font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 300;
   * ```
   */
  normal: { value: string }
  /**
   * Normal font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 400;
   * ```
   */
  medium: { value: string }
  /**
   * Medium font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 500;
   * ```
   */
  semibold: { value: string }
  /**
   * Semibold font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 600;
   * ```
   */
  bold: { value: string }
  /**
   * Bold font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight:700;
   * ```
   */
  extrabold: { value: string }
  /**
   * Extrabold font weight.
   * @type {string}
   * @example
   * ```css
   * font-weight: 800;
   * ```
   */
  black: { value: string }
}
